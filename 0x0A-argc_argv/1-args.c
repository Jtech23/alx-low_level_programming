#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @argc: argument count
 * Return: always 0
 */

int main(int argc, char *argv[])
{
(void) argv;
	printf("%d", argc - 1);
	printf("\n");
	return (0);
}
