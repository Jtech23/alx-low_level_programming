#include "main.h"
/**
 * times_table - Prints table 9
 * Return: Always 0
 */

void times_table(void)
{
	int a, b;

	b = 0;
	for (a = 0; a <= 9; a++)
	{
		_putchar(a * 0 );
		_putchar('\n');
	}
	for (a = 0; a <= 9; a++)
	{
		_putchar(a);
		_putchar('\n');
	}
	while (b <= 9)
	{
		_putchar(48);
		_putchar(c * 2)
		c++
		b++
	}
}
