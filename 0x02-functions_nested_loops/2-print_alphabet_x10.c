#include "main.h"
/**
 * print_alphabet_x10 - prints alphabet ten times
 */
void print_alphabet_x10(void)
{
	int ch;
	int i;

	for (i = 0; i++;)
	{
		for (ch = 'a'; ch <= 'z'; ch++)
		{
			_putchar(ch);
		}
	}
}
