#include "main.h"
/**
 * add - Function that adds two integers together and returns the result
 * @a: First integer
 * @b: second integer
 * Return: Returns the value of the addition of the two integers
 */

int add(int a, int b)
{
	return (a + b);
}
