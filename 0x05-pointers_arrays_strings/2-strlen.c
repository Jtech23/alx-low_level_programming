#include <stdio.h>
#include <string.h>
/**
 * _strlen - A function about the lnght of a string
 * @s: A character
 * Return: returns lenght of string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
